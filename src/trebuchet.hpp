#ifndef SRC_TREBUCHET_H
#define SRC_TREBUCHET_H

#include <algorithm>
#include <fstream>
#include <string>
#include <unordered_map>

int trebuchetDigits(std::string input_file);
int trebuchetNames(std::string input_file);

#endif /* SRC_TREBUCHET_H */
